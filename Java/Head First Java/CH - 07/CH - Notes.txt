
=> When overriding done the _JVM_ at runtime it knows which method to run when someone call it 


=> Change method return type only not consider overloading compiler just look at it as just overriding